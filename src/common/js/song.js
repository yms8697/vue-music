export default class Song {
  constructor ({id, name, singer, duration, album, imgUrl}) {
    this.id = id
    this.name = name
    this.singer = singer
    this.duration = duration
    this.album = album
    this.imgUrl = imgUrl
  }
}
